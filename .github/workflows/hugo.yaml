name: 发布 Hugo 网站到 GitHub Pages

on:
  # 在推送到默认分支时运行
  push:
    branches:
      - main

  # 允许手动触发
  workflow_dispatch:

# 设置 GITHUB_TOKEN 权限
permissions:
  contents: read
  pages: write
  id-token: write

# 并发控制：只允许一个部署正在进行
concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.160.0          # ← 更新到最新稳定版（2026 年 4 月）
      DART_SASS_VERSION: 1.99.0      # ← 推荐版本

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: 设置 GitHub Pages
        id: pages
        uses: actions/configure-pages@v5   # ← 更新到 v5/v6

      - name: 安装 Hugo CLI (Extended)
        run: |
          wget -O ${{ runner.temp }}/hugo.deb \
            https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb

      - name: 安装 Dart Sass（推荐方式）
        run: |
          mkdir -p ${{ runner.temp }}/dart-sass
          curl -L -o ${{ runner.temp }}/dart-sass.tar.gz \
            https://github.com/sass/dart-sass/releases/download/${DART_SASS_VERSION}/dart-sass-${DART_SASS_VERSION}-linux-x64.tar.gz
          tar -xzf ${{ runner.temp }}/dart-sass.tar.gz -C ${{ runner.temp }}
          echo "${{ runner.temp }}/dart-sass" >> $GITHUB_PATH

      - name: 安装 Node.js 依赖（如果有）
        run: |
          if [[ -f package-lock.json || -f npm-shrinkwrap.json ]]; then
            npm ci
          fi

      - name: 使用 Hugo 构建
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: |
          hugo \
            --gc \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"

      - name: 上传构建产物
        uses: actions/upload-pages-artifact@v3   # ← 推荐 v3（更稳定）
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5   # ← 已更新到 v5（这是你原脚本最需要改的地方）
  # ==================== QQ 邮箱通知 ====================
  notify:
    needs: deploy
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Send QQ Email Notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ://qq.com
          server_port: 465
          username: ${{ secrets.QQ_EMAIL }}
          password: ${{ secrets.QQ_SMTP_AUTH }}
          # 【核心设计】：状态符号化 + 仓库名 + 触发者，在通知栏就能看全
          subject: "${{ job.status == 'success' && '✅成功' || '❌失败' }} | Hugo: ${{ github.event.head_commit.message || '手动部署' }} (@${{ github.actor }})"
          to: ${{ secrets.QQ_EMAIL }}
          from: "Hugo 机器人 <${{ secrets.QQ_EMAIL }}>"
          secure: true
          # 邮件正文：提供更详细的链接和环境信息
          body: |
            ## 构建报告 ##
            状态：${{ job.status }}
            触发人：${{ github.actor }}
            提交信息：${{ github.event.head_commit.message }}
            仓库分支：${{ github.repository }} / ${{ github.ref_name }}
            
            查看详情：https://github.com{{ github.repository }}/actions/runs/${{ github.run_id }}
            在线预览：https://${{ github.repository_owner }}.github.io/
          html_body: false
